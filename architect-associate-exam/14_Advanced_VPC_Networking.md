# Topics
Covers some advanced features of VPC

# VPC flow logs
# Egress-only IGW
# VPC Endpoints
- VPC endpoints provide private IP access to public services
# VPC Endpoints (Gateway)
- Its like a routing trick. 
- For S3/DynamoDB you route to them direclty (via routing table) rather than through IGW 
# VPC Endpoints (Interface)
- A real ENI with a private IP
# VPC peering